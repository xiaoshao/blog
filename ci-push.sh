cd public
git config --global user.name "ResiWorks"
git config --global user.email "oyanglulu@gmail.com"
git add .
git commit -m "auto publi:ship: with https://github.com/resiworks/blog"
git push origin master
